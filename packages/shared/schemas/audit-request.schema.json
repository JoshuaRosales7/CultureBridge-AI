{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://culturebridge.ai/schemas/audit-request.json",
    "title": "AuditRequest",
    "description": "Request body for POST /api/audit — runs compliance & bias audit on a variant.",
    "type": "object",
    "required": [
        "variant_id"
    ],
    "properties": {
        "variant_id": {
            "type": "string",
            "pattern": "^var_[a-zA-Z0-9]{8,}$",
            "description": "ID of the variant to audit"
        },
        "strict_mode": {
            "type": "boolean",
            "default": false,
            "description": "If true, applies stricter bias detection thresholds"
        }
    },
    "additionalProperties": false
}